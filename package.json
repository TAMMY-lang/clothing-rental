{
  "name": "clothing-rental",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix backend exec prisma generate && npm --prefix backend run build",
    "start": "npm --prefix backend start"
  }
}
