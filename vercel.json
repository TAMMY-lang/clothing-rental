{
  "buildCommand": "cd frontend && rm -rf node_modules/.vite dist && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://clothing-rental-production-9bfe.up.railway.app/api/:path*"
    }
  ]
}
