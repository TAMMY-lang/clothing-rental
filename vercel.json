{
  "installCommand": "cd frontend && npm install",
  "buildCommand": "cd frontend && npx tsc -b && npx vite build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://clothing-rental-production-d13e.up.railway.app/api/:path*"
    }
  ]
}
